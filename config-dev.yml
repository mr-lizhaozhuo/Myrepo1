server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mr-lizhaozhuo/Myrepo1.git #仓库名字

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
version: "NEW"
